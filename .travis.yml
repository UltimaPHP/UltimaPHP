language: php

php:
  - 7.2

before_install:
  - sudo apt-get update
  - sudo apt-get install -y unzip
  - wget -O - https://ultimaphp.nyc3.digitaloceanspaces.com/ultimaphpmuls.zip | unzip - -d UOLocation
  - ls -la

script: php startserver.php

notifications:
  irc: "irc.youhide.com.br#ultimaphp"
